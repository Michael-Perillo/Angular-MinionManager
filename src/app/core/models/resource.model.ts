/** Influence earned per task completion, scaled by tier (all departments) */
export const INFLUENCE_PER_TIER: Record<string, number> = {
  petty: 1,
  sinister: 3,
  diabolical: 5,
  legendary: 8,
};
